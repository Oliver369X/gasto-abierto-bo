# Legacy scripts — DO NOT use in CI/prod

Sustituido por:

```
python -m scripts.cli fire pipeline --phase f1
python -m scripts.cli fire verify
python -m scripts.cli gasto …
```

Borrar después de 2026-08-14 si nadie los necesita.
